# 婴儿成长记录小程序 · 双后端迁移计划（Supabase / 微信云开发）

> 目标：新增一套微信云开发后端实现，与原 Supabase 后端**并存**，
> 通过一个配置常量决定启用哪一版。原 Supabase 代码一行不改，随时可切回。
>
> 状态看板：⬜ 未开始 ｜ 🟡 进行中 ｜ ✅ 已完成

---

## 〇、进度快照（2026-09-21）

> 一眼看现状的汇总，详细依据见第五节各阶段与第八节待确认清单。

**已完成（真机验收通过）**

| 部分 | 结论 |
|---|---|
| 阶段 0 切换骨架 | ✅ 2026-09-20 |
| 阶段 1 环境初始化 | ✅ 2026-09-21（探针已清理） |
| 阶段 3 认证 | ✅ 微信一键登录 → 建家庭 → 邀请家人 → 各记录页增删改查 |
| 阶段 5 云函数 | ✅ 数据导出、测试账号注销（测试账号验证） |

**代码已就绪、验收未闭环**

| 部分 | 缺什么 |
|---|---|
| 阶段 2 数据层 | 14 集合 + 50 条种子已验收；缺 `orderBy` 对 null 字段排序方向的实测 |
| 阶段 4 存储与照片 | 代码 + 接线 + 构建 + fileID 前缀 + `cloud://` bug 修复全完成；⛔ 真机验收被隐私指引审核阻塞 |
| 阶段 6 事务补偿 | `create_family` / `join_family_by_invite` 回滚已写；缺「重传 `data` + 注入失败验证」 |
| 阶段 7 真机验证与切换 | `BACKEND = 'cloud'` 已定稿、H5 回落已在产物层验证；缺照片链路 |

**当前唯一硬阻塞**

小程序《用户隐私保护指引》未声明相册/摄像头权限 → `chooseMedia` 报 `errno 112`，
照片上传链路完全无法真机验证。**代码侧无需改动，等审核通过。**

**待用户操作（控制台 / 微信开发者工具）**

- [ ] 删除云端已部署的 `ping` 云函数（源码与产物已删，云端那份还在）
- [ ] 重新上传部署 `data` 云函数（含阶段 6 补偿逻辑，`dist/build/mp-weixin/cloudfunctions/data`）
- [ ] 重新上传体验版（含隐私文案改动）
- [ ] 等隐私指引审核通过，之后补验照片链路
- [ ] 上线前人工通读 `pages/privacy/privacy.vue` 第一、二节（仍含云开发下已停用的手机号/邮箱表述）

**两版并存状态**：`src/services/supabase/**` 与 `supabase/**` 一行未改，
改 `src/config/index.js` 的 `BACKEND` 一个常量即可整体回滚。

**下一步**：迁移收尾（等隐私指引审核通过后补验照片链路）。
**第三期（公开推广 + 商业化）已由用户决定暂缓**，方案见《婴儿成长记录小程序-第三期开发需求文档.md》，
该文档按 Supabase 编写，日后落地前需做云开发适配（见下方第七节边界约定）。

**2026-09-21 补丁收尾**：已去掉一期 `families.invite_code`（含前端引用与 `create_family` 写入逻辑，
云开发侧需重传 `data` 云函数，Supabase 侧执行迁移 013）；疫苗页补齐月龄匹配（适龄高亮 + 置顶 +
按推荐月龄预填计划日期）。P1 的自定义提醒与 WHO 生长曲线、以及第三期内容，用户决定暂不开发。

---

## 一、背景与动机

现有后端是 Supabase，小程序要访问它必须：

1. 自己买服务器（Supabase 域名在国内无法直连，需 Nginx 反代）
2. 域名做 ICP 备案
3. 在微信后台配 request / uploadFile / downloadFile 合法域名

微信云开发走微信私有协议，**不需要域名、不需要服务器、不需要 ICP 备案**。
代价是数据层要重写一遍（云开发是文档型 NoSQL，权限模型与 Postgres RLS 完全不同）。

因此本计划的目标不是「换掉 Supabase」，而是「**叠加一套云开发实现，保留切换能力**」。

---

## 二、已锁定的决策

| 决策项 | 结论 | 影响 |
|---|---|---|
| 登录方式 | **只微信一键登录** | 云开发版不需要注册、不需要密码、不需要 SMTP |
| 数据来源 | 云开发环境**从空库开始** | 不写数据迁移脚本，家人重新建家庭 |
| 网页端（H5） | **自动回落 Supabase** | 云开发只在 mp-weixin 可用，H5 强制走 Supabase，平板网页端照旧 |
| 免费额度 | 微信侧 **40,000 资源点/月** | 小程序后台开通云开发即可，未发布阶段无需续费 |
| 迁移原则 | 代码复用优先、业务功能全保留、控制台操作只做一次 | 业务 service 层零改动 |

### 云开发版明确不含的功能

以下 3 项**只在 Supabase 版存在**，云开发版不做（切换回 Supabase 即可用）：

1. 手机号 + 密码登录
2. 绑定 / 修改邮箱
3. 邮件找回密码（Supabase 版走 SMTP，云开发版无此链路）

其余业务功能全部保留：喂养、睡眠、换尿布、成长、里程碑、照片、家庭与邀请码、
成员角色、疫苗、报告、数据导出、账号注销。

---

## 三、架构：一个切换点

### 现状（有利条件）

`src/services/supabase/index.js` 已经是页面与数据层之间的**唯一契约**，
文件头注释原文：

> 页面只与这里打交道，不关心底层是 HTTP 还是官方 SDK；
> 后续若要换回官方 SDK，只需替换本目录内的实现，页面代码无需改动。

全项目只有 **15 个文件** import 它：

| 类别 | 文件 |
|---|---|
| 业务 service（11） | account / baby / diaper / family / feeding / growth / milestone / photo / sleep / vaccine / vaccine-library |
| store（1） | `stores/auth.js` |
| util（1） | `utils/tracker.js` |
| 页面（2） | `pages/profile/profile.vue`、`pages/account/account.vue`（各自直接引 `isRecoveryEmailBound`） |

### 改造后

```
src/services/
  api.js            ← 新增：唯一切换点
  supabase/         ← 原样保留，一行不改
  cloud/            ← 新增：同形状实现
    index.js        契约聚合，导出与 supabase/index.js 完全同形的对象
    init.js         wx.cloud.init，仅 mp-weixin 执行
    session.js      复用 supabase/session.js 的实现（本地存储逻辑与后端无关）
    auth.js         微信 openid 登录
    db.js           把现有 PostgREST 语法翻译成云开发查询
    storage.js      wx.cloud.uploadFile / getTempFileURL / deleteFile
    functions.js    wx.cloud.callFunction
```

### 切换实现

`src/config/index.js` 新增：

```js
/** 后端选择：'supabase' | 'cloud' */
export const BACKEND = 'supabase'

/** 云开发环境 ID（开通云开发后填） */
export const CLOUD_ENV_ID = ''
```

新增 `src/services/api.js`：

```js
import { BACKEND } from '@/config'
import { supabase } from './supabase'
import { cloud } from './cloud'

// H5 平台不具备 wx.cloud，强制回落 Supabase
// #ifdef H5
export const api = supabase
// #endif
// #ifndef H5
export const api = BACKEND === 'cloud' ? cloud : supabase
// #endif

export default api
```

然后把 15 处 import 的路径从 `./supabase` 改成 `./api`（**只改路径，业务逻辑零改动**）。

### 回滚

把 `BACKEND` 改回 `'supabase'`，一行常量。
Supabase 侧的 `src/services/supabase/**`、`supabase/migrations/**`、`supabase/functions/**`
在整轮改造中**只读不写**。

---

## 四、契约清单（云开发版必须实现的接口）

来源：`src/services/supabase/index.js` 的导出面。

| 契约 | 现有实现 | 云开发实现 |
|---|---|---|
| `project.url` / `project.storageBucket` | 常量 | 占位值 |
| `db.select / selectOne / insert / insertSilent / upsert / remove / rpc / pickColumns` | PostgREST over HTTP | 通用云函数 `data` |
| `storage.uploadObject / createSignedUrl / createSignedUrls / removeObjects` | 私有桶 + signed URL | `wx.cloud.uploadFile` / `getTempFileURL` / `deleteFile` |
| `auth.signInWithWechat / signOut / refreshSession / fetchUser / checkConnection / currentUserId / accountToEmail / isRecoveryEmailBound` | GoTrue | 云函数 `login` |
| `auth.signUpWithPhone / signInWithAccount / updateEmail / requestPasswordReset` | GoTrue | **不做**，抛「当前后端不支持」 |
| `functions.invoke` | Edge Function | `wx.cloud.callFunction` |
| `session.get / set / clear` | uni.storage | **直接复用同一份实现** |
| `ApiError` | `http.js` | **直接复用** |
| `capabilities` | — | **新增**，见下 |

### 新增：`capabilities`（能力声明）

页面按「能力」渲染，而不是按「后端」写 if，这样以后切换不用再改页面。

```js
// Supabase 版
capabilities = { phoneLogin: true,  emailRecovery: true,  emailBinding: true,  wechatLoginCode: true }
// 云开发版
capabilities = { phoneLogin: false, emailRecovery: false, emailBinding: false, wechatLoginCode: false }
```

`wechatLoginCode`：微信登录前是否需要前端先 `uni.login` 换 code 交给后端。
Supabase 的 Edge Function 需要 code 才能 code2session；云开发的 openid 由微信侧注入云函数，不需要。
页面据此决定要不要走 `uni.login`，免得云开发下因这一步失败而登不进去。

页面通过 `@/services/api` 取 `capabilities`（取的是**当前生效后端**上的那份，
H5 强制回落 Supabase 时能力判断也跟着回落），不写 `BACKEND === 'cloud'` 这类分支。
另导出 `backendName`（`'supabase' | 'cloud'`）仅供文案区分（如「数据与备份」的说明）。

受影响页面：`pages/login/login.vue`、`pages/forgot-password/forgot-password.vue`、
`pages/account/account.vue`、`pages/profile/profile.vue`。

受影响 store action（`stores/auth.js`）：
`signInWithAccount` / `signUpWithPhone` / `savePhone` / `bindEmail` / `requestPasswordReset`
在云开发版下不参与流程，但代码保留。

---

## 五、阶段拆解

### 阶段 0 · 切换骨架 ✅（2026-09-20 完成）

**做什么**
- 新增 `src/services/api.js`
- `src/config/index.js` 加 `BACKEND`、`CLOUD_ENV_ID`
- 15 处 import 路径改为 `@/services/api`（含 2 个页面的 `isRecoveryEmailBound`）
- 新增空的 `src/services/cloud/index.js` 占位，导出与 supabase 同形的骨架

**验收**
`BACKEND = 'supabase'` 下 `npm run build:mp-weixin` 通过，真机功能与改前完全一致。

**风险**：低。不碰任何 Supabase 代码。

**实际结果**：13 个业务文件机械替换 `supabase.` → `api.` 共 102 处；2 个页面改 `isRecoveryEmailBound` 的 import 路径；构建通过。

---

### 阶段 1 · 云开发环境初始化 ✅（2026-09-21 完成，探针已清理）

**做什么**
- 小程序后台开通云开发，建环境，记录环境 ID
- `src/manifest.json` 的 `mp-weixin` 节点加 `cloudfunctionRoot`
- 新增 `src/services/cloud/init.js`：`wx.cloud.init({ env, traceUser: true })`
- `src/App.vue` 的 `onLaunch` 里按平台条件调用（`#ifdef MP-WEIXIN`）

**验收**：真机上 `wx.cloud.callFunction` 能调通一个 ping 云函数。

**需要你操作**：① 开通云开发（✅ 已完成）；② 上传云函数。
（全程只有这两次控制台操作，无按功能逐个开通的环节。）

**实际结果**（2026-09-20 ~ 09-21）
- `manifest.json` 加 `"cloudfunctionRoot": "./cloudfunctions/"`，已确认会透传进产物的 `project.config.json`。
- 云函数源码放 `src/cloudfunctions/`；uni-app 自己不会把它拷进产物，已在 `vite.config.js` 加了一个 `copy-cloudfunctions` 插件（仅 mp-weixin 生效）。
- 新增 `src/services/cloud/init.js`，`App.vue` 的 `onLaunch` 首行调用。
- 阶段 1 验收探针（`initCloud()` 里临时调 `ping` 云函数）已完成使命：阶段 2 的 `data`、阶段 3 的 `login`、
  阶段 5 的 `export-data` / `delete-account` 全部走通了 `wx.cloud.callFunction`，
  故已于 2026-09-21 **删除探针代码与 `src/cloudfunctions/ping/` 目录**（产物 `cloudfunctions/` 下也确认无残留）。
- ⏳ 待办：**到云开发控制台把已部署的 `ping` 云函数删掉**（源码已删，云端那份还留着）。

---

### 阶段 2 · 数据层 🟡（代码已完成，待部署云函数与验收）

**做什么**

1. **集合创建**：14 张表 → 14 个集合

2. **鉴权规则收敛**：库内 67 条 RLS 策略 → 云函数里的规则函数。
   核心是 `is_family_member(familyId)` / `is_family_owner(familyId)` 两个跨集合校验，
   云开发安全规则只认 `_openid`、做不了跨集合成员校验，
   因此**所有读写统一走云函数**，不在客户端直连数据库。

3. **通用云函数 `data`**：入参 `{ action, table, query }`，内部按表分派鉴权。
   （不做「每个操作一个云函数」，避免函数数量爆炸。）

4. **存储过程改写**：前端实际调用 **6 个**（库内共 13 个，其余未被前端使用）：

   | 存储过程 | 调用点 |
   |---|---|
   | `create_family` | `family.js:87` |
   | `join_family_by_invite` | `family.js:97` |
   | `create_family_invitation` | `family.js:106` |
   | `set_member_role` | `family.js:143` |
   | `set_my_nickname` | `family.js:178` |
   | `remove_family_member` | `family.js:187` |

5. **`cloud/db.js` 吃掉语法差异** —— 已盘点业务层实际用到的 PostgREST 语法，面很窄：

   | 语法 | 出现处 | 云开发翻译 |
   |---|---|---|
   | 等值 `match` | 全部 select / remove | `where(obj)` |
   | 数组 = 同字段多条件（range） | feeding / diaper / milestone / photo | `_.gte` + `_.lt` 组合 |
   | `in.(a,b)` | `family.js:75` | `_.in` |
   | `gte.xxx` | `sleep.js:51` | `_.gte` |
   | `order: 'x.desc'` / `'x.asc'` | 多处 | `orderBy` |
   | `order: 'x.desc.nullslast'` | `vaccine.js:67` | ⚠️ 云开发无 nullsLast，客户端补排 |
   | `limit` / `offset` | 多处 | `skip` / `limit` |
   | `count: true` | `photo.js:92` | `count()` |
   | `insertSilent` | `utils/tracker.js`（app_logs） | 直接 `add`，不回读 |

   **结论：薄适配可行，13 个业务 service 一行都不用改。**

**验收**：把 `BACKEND` 切到 `cloud`，在真机上跑通「注册 → 建家庭 → 记一条喂养 → 看列表」。
（注：认证在阶段 3 才落地，所以阶段 2 的验收只做「云函数侧」——建集合、灌字典、逐个动作调用 data 云函数。）

**已拍板的三项决策**
| 问题 | 结论 |
|---|---|
| 集合主键 `_id` 与业务层 `id` 的差异 | 在云函数内透明映射，业务层无感 |
| 14 个集合怎么建 | 加一次性云函数 `init-db`（顺带灌疫苗字典种子） |
| 交付节奏 | 阶段 2 单独验收，再做 3~5 |

**实际结果**（2026-09-20）

产出 3 个新文件 + 1 处接线，业务层确实一行未改：

| 文件 | 说明 |
|---|---|
| `src/cloudfunctions/init-db/index.js` | 一次性：建 14 个集合（已存在则跳过，用 `count()` 复核）+ 给 `vaccine_library` 灌 50 条种子（一类 22 + 二类 28，集合为空才写）。可重复执行 |
| `src/cloudfunctions/data/index.js` | 通用数据云函数：6 个 action（select / insert / insertSilent / upsert / remove / rpc）+ 鉴权矩阵 + `_id`↔`id` 映射 + 6 个存储过程的 JS 重写 |
| `src/services/cloud/db.js` | 与 `supabase/db.js` 同形的翻译层：PostgREST 语法 → 云函数入参 |
| `src/services/cloud/index.js` | `db` 节点由 `pending()` 换成真实实现（auth / functions / storage 仍是 pending，留给阶段 3~5） |

关键实现说明：
- 鉴权矩阵全部收在 `data` 云函数内，身份取 `cloud.getWXContext().OPENID`，前端传什么都不信。
- `families` 的读做了交集处理：客户端给 id 时与「我是 active 成员的家庭」取交集；不给 id 时直接返回我的全部家庭。
- `scheduled_date.desc.nullslast`：云开发 `orderBy` 表达不了 null 位置，`cloud/db.js` 在客户端对这一页补排（唯一调用方疫苗列表取的是同一婴儿全量记录，补排准确）。
- `pickColumns` 是纯函数，直接复用同一份实现，不占云函数调用。

**需要你操作（都属于「部署前先确认」范畴，等你点头再做）**
1. **先把云函数超时时间调大**（见下方「云函数超时时间」一节），否则第 2 步必然失败。
2. 微信开发者工具导入 `dist/build/mp-weixin`，右键 `cloudfunctions/init-db` → 「上传并部署：云端安装依赖」。
3. 在「云函数 → init-db → 云端测试」用空参数 `{}` 调一次，看返回清单：`collections.created / existed / failed` 与 `vaccine.inserted=50`。**先把返回内容贴给我确认。**
4. 确认无误后同样上传 `cloudfunctions/data`。
5. 云开发控制台「数据库」里应为 14 个集合，`vaccine_library` 里应有 50 条。

#### 云函数超时时间（必须做一次的配置）

腾讯云函数**默认超时 3 秒**，超时后报 `Invoking task timed out after 3 seconds`（statusCode 433），
函数被强制终止、已完成的数据库写入保留。

需要改的地方：**云开发控制台 → 云函数 → 选中函数 → 配置 → 超时时间 → 改成 20 秒 → 保存**
（若保存后重跑仍报 3 秒，再对该函数执行一次「上传并部署」让配置重新应用）。

需要改的函数及理由：

| 函数 | 建议 | 理由 |
|---|---|---|
| `init-db` | 20 秒 | 一次性建 14 个集合 + 插 50 条种子。代码已改成 5/10 条一批并发，但首次初始化仍可能超 3 秒 |
| `data` | 20 秒 | 真机上一次操作会连续查多次（如 `listActiveFamilyIds` → 逐表查询）。现阶段串行往返多，3 秒很容易撞线 |
| `login` | 10 秒 | 查 `profiles` + 可能建档，正常 1 次往返，但冷启动本身就要 1~2 秒 |

已踩过的坑：2026-09-20 首次调用 `init-db` 报 433 超时（当时未调超时时间、且代码是串行 78 次往返）。
`init-db` 全程幂等（已存在的集合跳过、字典非空则跳过），**超时后直接重跑即可，不会产生脏数据**。

---

### 阶段 3 · 认证（已因决策缩水）✅（2026-09-21 真机验收通过）

**做什么**
- 云函数 `login`：取 `wxContext.OPENID` → 在 `profiles` 集合 upsert 一条 → 返回用户信息
- `cloud/auth.js`：
  - `signInWithWechat(code)` → 改为直接调 `login` 云函数（云开发天然有 openid，无需 code 换 session）
  - `fetchUser` / `currentUserId` / `checkConnection` / `signOut` 实现
  - `phoneLogin` 三项抛「当前后端不支持」
- `capabilities` 注入两个后端
- 登录页 / 忘记密码页 / 账号页 / 我的页按能力降级渲染

**为什么大幅缩水**：云函数天然拿得到调用者 openid，身份本身可信，
**不需要 JWT / refresh token / access_token 续期**，也不需要 `users` 集合和密码哈希。
原 `wechat-login` 与 `phone-login` 两个 Edge Function 合并成一个 `login` 云函数。

**顺带解决的历史包袱**：SMTP 535 那个坑彻底消失；
「忘记密码」从「发邮件找回」变成「微信登录后直接重设」，用户少一步。
（但按决策，云开发版本期不做密码，所以此项在本期不体现。）

**验收**：真机上微信授权登录 → 直接进入建家庭流程。

**实际结果**（2026-09-20）

| 文件 | 说明 |
|---|---|
| `src/cloudfunctions/login/index.js` | `OPENID` → 查 `profiles`（`_id` 即 openid）→ 没有就建档（`nickname`/`avatar_url`/`wechat_openid`/`wechat_unionid`/`updated_at`）→ 返回 `{ ok, user, created }`；首次登录并发撞 `_id` 有兜底（撞了就回查一次）。`profiles` 没有 `created_at` 列，只写 `updated_at` |
| `src/cloudfunctions/login/package.json` | 依赖 `wx-server-sdk` |
| `src/services/cloud/auth.js` | 与 `supabase/auth.js` 同形 12 方法，页面/store 一行不改 |
| `src/services/cloud/index.js` | `auth` 节点由 8 个 `pending()` 换成真实实现 |
| `src/services/supabase/index.js` | 新增 `capabilities` |
| `src/services/api.js` | 新增 `capabilities`、`backendName` 两个导出 |

关键实现说明：
- **`access_token` 占位**：`session.normalize()` 要求 `access_token` 非空（否则整份登录态被判成 null，
  而 `stores/auth.js` 的 `isLoggedIn` 也是看它）。云开发没有令牌，用 `user.id`（openid）占位，
  这个值不会发往任何服务端——云函数只认 `cloud.getWXContext().OPENID`。
- **`profiles` 的键就是 openid**：login 云函数写 `_id = openid`，阶段 2 的 `data` 云函数对
  `profiles` 也强制 `_id = openid`（select 强制 `where id = 调用者`、upsert 强制 `doc._id = 调用者`），
  两边一致，`store.savePhone()` 等调用无需改动。
- **`signOut` 只清本地**：身份来自微信，服务端没有会话可吊销。
- **`refreshSession` 直接返回当前 session**：云开发不存在过期与续期。
- **`fetchUser` = 再调一次 login 云函数**并回写本地。
- **`checkConnection` = 能否调通 login 云函数**。

页面降级（按 `capabilities`，不写后端分支）：
| 页面 | 改动 |
|---|---|
| `pages/login/login.vue` | 云开发下隐藏手机号表单、注册切换、底部说明与「或使用手机号登录」分隔线，只留微信按钮；`wechatLoginCode=false` 时跳过 `uni.login`，失败文案也不提「手机号登录」 |
| `pages/profile/profile.vue` | 账号卡片右侧改为「微信账号」（不再显示「未绑定邮箱」红字）；退出登录弹窗文案改为「重新微信授权登录」 |
| `pages/account/account.vue` | 隐藏「找回密码邮箱」行、未绑定提示、待确认邮箱刷新按钮、整个「绑定真实邮箱」卡片；当前账号显示「微信账号」；「数据与备份」说明按 `backendName` 换文案（不再写死 Supabase） |
| `pages/forgot-password/forgot-password.vue` | 无需改：唯一入口在登录页，已被隐藏；该页整页仍是 Supabase 邮箱找回流程 |

构建验证：`BACKEND='supabase'` 与临时切 `'cloud'` 各构建一次均通过；
切 cloud 时产物里 `cloudfunctions/login/index.js` 已被拷入，`services/cloud/auth.js` 真实编译产出，
且产物中不再有 `云开发后端尚未实现：auth` 字样。

**真机验收结果**（2026-09-21）：微信一键登录通过 → 建家庭通过 → 邀请家人通过 → 各记录页（喂养/睡眠/便便/生长/疫苗/里程碑）增删改查全部通过。
阶段 1 的 ping 探针即因此确认完成使命并删除。

**需要你操作（属于「部署前先确认」范畴）** —— 已完成 ✅
1. 微信开发者工具导入 `dist/build/mp-weixin`，右键 `cloudfunctions/login` → 「上传并部署：云端安装依赖」。
2. 真机点「微信一键登录」，看是否直接进入建家庭流程。

---

### 阶段 4 · 存储与照片 🟡（代码已完成并接线、构建通过、前缀已填、bug 已修；⛔ 真机验收被隐私指引审核阻塞）

**做什么**（`cloud/storage.js`，`photo.js` 的 24 处调用零改动）

| 现有 | 云开发 | 状态 |
|---|---|---|
| `uploadObject`（uni.uploadFile multipart） | `wx.cloud.uploadFile` | ✅ |
| `createSignedUrl` | `wx.cloud.getTempFileURL` | ✅ |
| `createSignedUrls`（批量） | `wx.cloud.getTempFileURL`（一次最多 50 个，已自动分批） | ✅ |
| `removeObjects` | `wx.cloud.deleteFile` | ✅ |

**为何不用 base64 中转**：一页 20 张照片约 20MB，云函数中转不可行，照片必须进云存储。

**已完成的代码改动**（2026-09-20）
1. 新增 [storage.js](file:///e:/workspace/个人项目测试/chenfen/src/services/cloud/storage.js)：与 `supabase/storage.js` 同形，业务层（photo / baby / milestone）零改动。
2. [index.js](file:///e:/workspace/个人项目测试/chenfen/src/services/cloud/index.js) 的 `storage` 节点由 4 个 `pending()` 换成真实实现。
3. [config/index.js](file:///e:/workspace/个人项目测试/chenfen/src/config/index.js) 新增 `CLOUD_FILE_ID_PREFIX`。
4. 构建验证通过，产物中已无 storage 的 NOT_IMPLEMENTED 分支。

**关键差异：数据库存的是相对路径，云开发要的是文件 ID**

Supabase 的 `storage_path` 是桶内相对路径（如 `{familyId}/{babyId}/xxx.jpg`），
云开发的存储接口只认文件 ID，形如：

```
cloud://<环境ID>.<存储桶ID>/<相对路径>
```

环境 ID 已知（`cloudbase-d3gmqwgbx043c78eb`），但**存储桶 ID 是随机分配的，无法推导**，
只能从控制台复制一次并写进 `CLOUD_FILE_ID_PREFIX`。
`storage.js` 里 `toFileId()` 负责拼接；已是 `cloud://` / `http(s)://` 的值原样透传。

`CLOUD_FILE_ID_PREFIX` 留空时，所有云存储调用直接抛 `STORAGE_NOT_CONFIGURED`（不静默失败）。

**已修的 bug（2026-09-21）**
`storage.js` 的 `toFileId()` 原本拼出的是 `cloudbase-xxx.xxx/path`，**漏了 `cloud://` 协议头**，
不是合法文件 ID（官方文档与示例一律是 `cloud://<环境ID>.<存储桶ID>/<路径>`）。
而 `CLOUD_FILE_ID_PREFIX` 按官方向导本来就是「复制 File ID 后去掉 `cloud://`」的部分，
所以拼接时必须补回协议头。若不复现真机就会表现为「选图成功但图片全部显示不出来」。
已改为 `return \`cloud://${CLOUD_FILE_ID_PREFIX}${path}\``，`delete-account` 云函数里同规则。

**需要你操作（属于「部署前先确认」范畴）**

1. ~~复制云存储 File ID~~ ✅ 已拿到：`cloud://cloudbase-d3gmqwgbx043c78eb.636c-cloudbase-d3gmqwgbx043c78eb-1317399262/...`
   ⇒ `CLOUD_FILE_ID_PREFIX = 'cloudbase-d3gmqwgbx043c78eb.636c-cloudbase-d3gmqwgbx043c78eb-1317399262/'`（2026-09-20 已填入）
2. 云存储权限：**不需要改、不需要升级套餐**。云存储默认是「公有读」（腾讯云官方文档原文：默认情况下的权限就是公有读），
   `getTempFileURL` 因此对任意用户都能换出 https 链接，另一位家长能看到对方上传的照片。
   「仅创建者可读写」这类更严格的自定义安全规则才需要动权限页，本项目用不上。
3. 真机验收：上传照片 → 列表显示 → 详情页查看 → 删除生效；宝宝头像同理。

**⛔ 当前阻塞**（2026-09-21 用户反馈「图片的现在测试不了，上传不了，也用不了相机」）

选图/拍照在微信侧就被拦下，报 `chooseMedia:fail api scope is not declared in the privacy agreement, errno: 112`，
根本没走到云存储上传。原因是小程序《用户隐私保护指引》尚未声明「收集你选中的照片或视频信息」。
**代码无需改**（基础库会自动弹官方隐私授权弹窗），等指引审核通过后即可继续验收。
审核期间阶段 5、6 的代码开发照常推进。

**待实测确认（不自行假设）**

| 待确认 | 现状/依据 |
|---|---|
| 临时 URL（https）能否用于 `<image src>` | 微信官方文档称「组件支持传入云文件 ID」，CloudBase 文档称 `<image>` 只认 https。本项目统一返回 https，**两种说法下都能用**，仍要真机确认一次 |
| `uni.previewImage` 能否用云文件 ID 预览 | 同上，本项目传的是 https，规避该差异 |
| 导出/报告页 canvas 取图 | `report.vue` 用 `loadImage(url)`，必须是真实 https 链接，不能是 `cloud://` ⇒ 这也是统一返回 https 的原因 |


---

### 阶段 5 · 云函数 ✅（2026-09-21 真机验收通过：数据导出、账号注销）

| 现有 Edge Function | 云开发 | 状态 |
|---|---|---|
| `export-data` | 云函数，逻辑照搬，只换数据访问 | ✅ 已写 |
| `delete-account` | 云函数，逻辑照搬（**云开发没有外键级联，必须手动逐集合删**） | ✅ 已写 |
| `wechat-login` | 并入阶段 3 的 `login` | ✅ 阶段 3 已完成 |
| `phone-login` | 本期不做 | — |

**已完成的代码改动**（2026-09-21）

1. 新增 [functions.js](file:///e:/workspace/个人项目测试/chenfen/src/services/cloud/functions.js)：客户端调用层，与 `supabase/functions.js` 同形，
   `invoke(name, body)` 走 `wx.cloud.callFunction`。云开发侧**没有 Authorization 头 / withAuth 选项**——OPENID 由微信注入云函数，前端伪造不了。
2. [index.js](file:///e:/workspace/个人项目测试/chenfen/src/services/cloud/index.js)：`functions.invoke` 由 `pending()` 换成真实实现；
   至此 cloud 版**已无任何 NOT_IMPLEMENTED 占位**，无用的 `pending()` 辅助函数已删除。
3. 新增 [export-data/index.js](file:///e:/workspace/个人项目测试/chenfen/src/cloudfunctions/export-data/index.js) + `package.json`：
   按 OPENID 推导「我是 active 成员的全部家庭」，逐个集合分页（1000/页）导出 10 张业务表，`_id` 统一映射成 `id`。
4. 新增 [delete-account/index.js](file:///e:/workspace/个人项目测试/chenfen/src/cloudfunctions/delete-account/index.js) + `package.json`。
5. 构建验证通过（`BACKEND='cloud'`），产物 `cloudfunctions/` 下已含 `export-data`、`delete-account`，
   客户端 `services/cloud/functions.js` 未被 DCE 剔除。

**关键差异：云开发没有外键级联，删家庭要手动逐个集合清**

Supabase 版删 `families` 靠 `on delete cascade` 一次清掉 10 张子表、删 `auth.users` 再级联清掉
`family_members`（其他家庭的成员关系）与 `profiles`。云开发没有这些，所以 `delete-account` 里显式做：

| 步骤 | 云开发实现 |
|---|---|
| 1. 校验身份 | `cloud.getWXContext().OPENID`（等价于 Supabase 的解析 access_token） |
| 2. 找「我是 owner 的家庭」 | `family_members` where `user_id=openid, role='owner', status='active'` |
| 3. 先收集存储路径 | `baby_photos.storage_path` / `babies.avatar_url` / `milestones.photo_url`（删表后就查不到了） |
| 4. 删家庭数据 | 8 张家庭子表 + `family_invitations` + `family_members` + `families`，按 `family_id in (...)` 逐个 remove |
| 5. 删存储对象 | `cloud.deleteFile`（50 个一批，拼 `cloud://` 前缀），尽力而为、失败只记日志 |
| 6. 删账号痕迹 | `family_members` where `user_id=openid`（别人家庭里的成员关系）+ `profiles` where `_id=openid` |

`app_logs` 在 Supabase 侧是 `on delete set null`（日志保留、只把 `user_id` 置空），云开发同样**保留日志行不动**，保持一致。

`export-data` 侧的差异：`user.email` / `user.created_at` 恒为 `null`（云开发没有邮箱体系，`profiles` 也没有 `created_at` 列）。

**需要你操作** —— 已完成 ✅

1. 部署两个云函数：微信开发者工具打开 `dist/build/mp-weixin` → 右键 `cloudfunctions/export-data`、`cloudfunctions/delete-account`
   → 「上传并部署：云端安装依赖」（与阶段 2/3 的 `data`、`login` 同样操作）。
2. 把 `delete-account` 的超时时间调大（云开发控制台 → 云函数 → delete-account → 配置 → 超时时间，默认 3 秒不够，建议 20 秒）。
3. 真机验收：用**测试账号**跑一次「数据导出」+「账号注销」（注销是破坏性操作，务必用测试账号）。

**真机验收结果**（2026-09-21）：数据导出通过、账号注销（测试账号）通过。

---

### 阶段 6 · 事务补偿 🟡（代码已完成；待真机验证）

`join_family_by_invite` 是「查邀请码 → 插成员 → 作废邀请码」三步，
Postgres 有原子事务，**云开发没有跨文档事务**。

**做了什么**（2026-09-21，两处）

| 函数 | 原风险 | 补偿措施 |
|---|---|---|
| `join_family_by_invite` | 作废邀请码失败 ⇒ 「码还能用、但人已进组」，同一码可反复加入 | 失败时回滚成员关系：新建的行直接删、复活的旧行还原成调用前的 `role`/`status`，然后抛错让用户重试 |
| `create_family` | 第二步写 owner 成员关系失败 ⇒ 残留「没有任何成员的家庭」（谁也看不见的垃圾数据） | 失败时删掉刚建的 family 行，然后抛错 |

**逐项审查结论（其余多步写入是否同样有问题）**

| 位置 | 是否多步 | 结论 |
|---|---|---|
| `actionInsert` / `actionUpsert` / `actionRemove` | 逐行循环写 | 现有全部调用方（baby / growth / feeding / sleep / diaper / vaccine / photo / milestone）**都只传单行**，不存在「批量写一半」的场景；`actionRemove` 也是先对全部行做完鉴权再删，不会半途被权限拒绝 |
| `create_family_invitation` / `set_member_role` / `set_my_nickname` / `remove_family_member` | 否 | 单次写入，天然原子 |
| `login` 云函数补 `profiles` | 否 | 单次写入（撞 `_id` 已存在的兜底重查也只是一次读） |
| `delete-account` 多集合删除 | 是（跨 12 个集合） | **删除顺序是「先子表、后 families」**，因此中途失败后重跑仍能找到家庭并继续清干净；只有云存储文件是尽力而为（与 Supabase 版一致，失败只记日志） |

**需要你操作**：把改动后的 `data` 云函数**重新上传部署一次**（补偿逻辑在它里面，不重传则云端仍是旧版）。
部署入口：微信开发者工具 → `dist/build/mp-weixin` → 右键 `cloudfunctions/data` → 「上传并部署：云端安装依赖」。

**验收**（待做）：人工注入第二步失败（临时把 `family_invitations` 的 update 改成抛错），
确认 `family_members` 回到调用前的状态、邀请码仍可用。

---

### 阶段 7 · 真机验证与切换 🟡（两个决策已定、H5 回落已验证；只剩照片链路待验）

**做什么**
- `BACKEND = 'cloud'`，用体验版真机跑全链路：
  登录 → 建家庭 → 邀请家人 → 各记录页增删改查 → 照片 → 报告 → 导出 → 注销
- H5 端确认自动回落 Supabase 且功能正常
- 确认后决定是否把 `BACKEND` 默认值改为 `'cloud'`

**决策结果**（2026-09-21）
1. **`BACKEND` 默认值定为 `'cloud'`** —— 微信小程序走云开发，H5 由条件编译回落 Supabase。
   两边代码都保留、可随时整体回滚（把常量改回 `'supabase'` 即可）。已在 [config/index.js](file:///e:/workspace/个人项目测试/chenfen/src/config/index.js) 的注释里写明。
2. **`pages/privacy/privacy.vue` 第三节按云开发改写**（原文案写死「数据存储于 Supabase」，语义已不符）：
   存储方改为「腾讯云（微信云开发）」；「行级权限隔离」改为「所有读写都经服务端校验」；
   照片临时链接按实际有效期标注为 2 小时；密钥那条改为「前端不含可直接访问数据库的凭证」。
   ⚠️ 该页属法律文案，仍建议你上线前通读一遍第一节（信息收集）与第二节（使用方式）——
   里面还留着「手机号」「绑定邮箱」「找回密码」，云开发下已经不做这些了（多写比少写安全，未擅自删除）。

**H5 回落已验证**（2026-09-21，构建产物层面）
- `npm run build:h5` 通过；产物 JS 里能找到 Supabase 地址，**完全没有 `callFunction` 调用**（云开发客户端代码被 DCE 剔除）。
- 结论：`src/services/api.js` 的 `// #ifndef H5` 回落生效——H5 端无论 `BACKEND` 配成什么都走 Supabase。
- 仍需你在浏览器里实际点一遍确认功能正常（H5 与小程序数据不互通，是已知并接受的）。

**真机全链路进度**（2026-09-21）

| 环节 | 状态 |
|---|---|
| 微信一键登录 | ✅ 通过 |
| 建家庭 / 邀请家人 | ✅ 通过 |
| 各记录页增删改查 | ✅ 通过 |
| 数据导出 | ✅ 通过 |
| 账号注销（测试账号） | ✅ 通过 |
| 照片上传 / 显示 / 删除、宝宝头像 | ⛔ 被隐私指引审核阻塞 |
| 报告页（依赖照片与 canvas 取图） | ⬜ 随照片一并验 |

**验收**：家人用体验版正常使用一周无阻断问题。

---

## 六、风险清单

| 风险 | 说明 | 应对 |
|---|---|---|
| 云开发安全规则能力边界 | 跨集合成员校验是否可完全在云函数外实现存疑 | 统一走云函数，不依赖安全规则做业务鉴权 |
| 云存储临时 URL 域名白名单 | ⏳ 仍待实测（被隐私指引审核阻塞，选图接口还没放行） | 阶段 4 真机验证，不通过则改走云函数代理 |
| `nullslast` 排序 | 云开发不支持 | 客户端补排（仅 `vaccine.js` 一处） |
| 无跨文档事务 | 邀请码等三步写入 | ✅ 阶段 6 补偿逻辑已写（待注入失败验证） |
| 免费额度到期 | 未发布阶段无需续费；发布后第 15 天起需 19.9 元/月 | 本期不发布，不影响 |
| H5 与小程序后端不一致 | 两版数据不互通 | 已知并接受；H5 仅自用 |
| 云函数需手动改超时 | `delete-account` 删多个集合、`init-db` 建 14 个集合，默认 3 秒不够 | 已分别调到 20 秒（控制台一次性配置） |

---

## 七、两版并存的边界（Supabase 保底措施）

用户明确要求：**必须始终存在两版实现——`supabase` 与云开发（云开发只针对微信小程序端）**。

- `src/services/supabase/**` —— 整轮改造中一行不改，任何时刻都能接管
- `supabase/migrations/**`（12 个文件）—— 不动
- `supabase/functions/**`（4 个）—— 不动
- `supabase/backups/**` —— 不动
- **`BACKEND` 默认值 = `'cloud'`**（阶段 7 决策，2026-09-21）：小程序端走云开发，H5 端由条件编译强制回落 Supabase。
  要整体回滚，只改这一个常量即可，不需动任何其他代码
- 云开发版**不改 `supabase/**` 下任何实现**；对现有文件的改动仅限三类：
  config 加常量（阶段 0）、`@/services/api` import 路径替换（阶段 0）、
  以及按 `capabilities` 做的页面降级渲染（阶段 3，不改变 Supabase 后端下的行为）
- 两版的分工与能力差异（云开发无手机号登录、无邮箱找回密码、无邮箱体系）由 `capabilities` 声明，
  页面据此渲染，不写后端分支

---

## 八、待确认清单

- [ ] 阶段 4 实测：云存储临时 URL 能否直接用于 `<image src>`（⛔ 被隐私指引审核阻塞）
- [ ] 阶段 4 实测：`uni.previewImage`、`report.vue` 的 canvas `loadImage` 能否吃 https 临时链接（同上阻塞）
- [x] 阶段 2 实测：云函数内 `_.in` 与同字段多操作符（`_.and`）查询的真实行为 —— 各记录页增删改查与导出（跨集合 `_.in`）真机通过，行为符合预期
- [ ] 阶段 2 实测：云开发 `orderBy` 对缺失/null 字段的排序方向（`cloud/db.js` 已用客户端补排兜底，需确认补排结果与 Supabase 侧一致）
- [x] 阶段 2 决策：`app_logs` 在云开发下保留「只写不可读」——`data` 云函数里 `insertSilent` 放行、`select` 直接拒绝
- [x] 阶段 3 实测：真机微信一键登录 → `store.loadFamilyContext()` 正常（2026-09-21 通过）
- [x] 阶段 5 实测：数据导出、测试账号注销（2026-09-21 通过）
- [x] 阶段 7 决策：`BACKEND` 默认值改为 `'cloud'`
- [x] 阶段 7 决策：`pages/privacy/privacy.vue` 第三节按云开发改写（2026-09-21 完成；第一、二节含已停用的手机号/邮箱表述，未擅自删除，建议人工通读）
- [ ] 阶段 6 实测：人工注入第二步失败，确认 `join_family_by_invite` 回滚生效
- [ ] 控制台清理：删掉已部署的 `ping` 云函数（源码已删）；重传 `data` 云函数（含阶段 6 补偿逻辑）

---

## 九、多端支持评估（2026-09-21 记录，暂不实施）

> 起因：用户询问「打包 Android / iOS、发支付宝小程序」时，当前云开发方案能否直接适配。
> 本节只作结论留档，**不产生任何代码改动**。

### 结论

**不能直接适配。** 云开发的身份基石是微信 openid，而 openid 只存在于微信小程序里。

### 各端现状（`BACKEND = 'cloud'` 前提下）

| 目标端 | 现状 | 说明 |
|---|---|---|
| 微信小程序 | ✅ 可跑 | 唯一被支持的端 |
| H5 | ⚠️ 可跑但走另一套后端 | `src/services/api.js` 的 `#ifndef H5` 强制回落 Supabase |
| 支付宝小程序 | ❌ 不可用 | `wx.cloud` 不存在，且拿不到 openid |
| Android / iOS App | ❌ 不可用 | 同上；App 端没有 openid 这个概念 |

### 根因（代码位置）

1. `src/services/api.js` 的兜底**只写给了 H5** —— App 与支付宝小程序会落到 `cloud` 分支
2. `src/services/cloud/{auth,db,storage,functions}.js` 四处均有 `#ifndef MP-WEIXIN` 分支，
   非微信小程序端直接 reject `CLOUD_UNAVAILABLE`
3. `src/cloudfunctions/data/index.js` 的鉴权链（`assertMember`）完全基于
   `cloud.getWXContext().OPENID`；`family_members.user_id` / `profiles._id` 存的就是 openid

### 打包 Android 的实际障碍（按层拆开）

| 层 | 障碍 | 性质 |
|---|---|---|
| 1 代码 | `BACKEND = 'cloud'` + 上述 `#ifndef MP-WEIXIN` 直接拒绝；`api.js` 兜底未覆盖 App | 好改 |
| 2 数据 | 切 Supabase 后 App 连的是空库 —— 现有数据（家庭 / 宝宝 / 各记录）全在云开发；照片在微信云存储 | **根本问题，最重** |
| 3 登录 | `manifest.json` 的 `modules` 与 `distribute.sdkConfigs` 均为空；App 端微信登录需微信开放平台「移动应用」并完成开发者认证（收费，以官方为准），且**小程序 openid ≠ App openid**，须靠 unionid 才能识别同一用户，现有 `wechat-login` Edge Function 只认小程序 appid/secret | 需外部账号 + 改 Edge Function |
| 4 功能 | 疫苗订阅提醒在 `pages/vaccine/vaccine.vue` 有 `#ifdef MP-WEIXIN` 守卫 → App 端静默失效；数据导出在 `pages/account/account.vue` 的 `#ifndef MP-WEIXIN` 分支是给 H5 写的 Blob 下载，App webview 内不生效 | 需补实现 |
| 5 打包上架 | `manifest.json` 的 `appid` 为空（App 打包需 DCloud AppID）；Android 上架国内商店需软著 + 移动应用备案；iOS 需 Apple 开发者账号 | 非技术成本 |

### 两条关键判断

**判断一：能「一份后端服务所有端」的是 Supabase，不是云开发。**

`src/services/supabase/http.js` 基于 `uni.request`，**全程零平台条件编译**，
H5 / Android / iOS / 各小程序都能发请求。云开发则把端绑死在微信生态内 ——
这不是技术做不到，是产品定位如此。腾讯云 CloudBase 虽有 App/Web SDK，
但那是另一套身份体系（匿名 / 自定义 / 微信开放平台），拿不到小程序 openid，
同一用户会变成另一个身份，看不到自己在小程序里建的家庭。

**判断二：域名与备案是「小程序」带来的门槛，不是后端选型带来的。**

- 微信 / 支付宝小程序的 request 合法域名**必须已备案**，`*.supabase.co` 不是自己的域名，配不进白名单。两条出路：
  - **自建域名反向代理**：买域名 **且需一台境内服务器**（这是 ICP 备案的前提，光买域名备不了案），通常需等待数周
  - **云函数转发**：云函数出网不受小程序白名单约束，**无需域名** —— 本项目 `src/cloudfunctions/data` 已具备这套转发 + 鉴权框架，是零域名成本方案
- 反过来说，**只做 App / H5 时 Supabase 无需域名、无需备案，今天即可直连**（App 端原生网络无白名单限制）。

### 当前决定（2026-09-21）

- **H5 不作为对外产品**，项目只保留微信小程序一端；`src/services/api.js` 的 H5 回落逻辑保持不动
- 多端扩展（Android / iOS / 支付宝小程序）**暂不实施**，本节判断作为日后决策依据留档
- 若要重启多端，建议顺序为：先定「数据最终放哪」→ 再做数据迁移 → 再补 App 端登录与缺失功能 → 最后打包上架
